# Copy this file to `.env.local` and fill in the real values from the
# Supabase dashboard → Project Settings → API. Never commit .env.local.

# Project URL, e.g. https://trubcyitreqrvkqmumen.supabase.co
VITE_SUPABASE_URL=

# The PUBLISHABLE (anon) key — safe to ship to the browser; RLS protects data.
VITE_SUPABASE_ANON_KEY=
